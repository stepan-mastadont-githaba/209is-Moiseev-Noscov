# 209is-Moiseev-Noscov

###      21.04.25
git init -- инициирует новый репозиторий
git clone <url> -- клонирует удаленный репозиторий по url
git add <file> -- добавляет файл для последующего коммита. git add . для всех изменений
git commit -m "*сообщение*" -- коммитит указанные изменения
git status -- текущее состояние рабочей директории
git log -- выводит историю коммитов текущей ветки
git diff -- показывает изменения в файлах, которые еще не добавлены
git branch -- список локальных веток, git branch <имя> чтобы создать новую
git checkout <ветка> -- переключает на указанную ветку
git merge <ветка> -- сливает указанную ветку с текущей веткой
git pull <remote> <branch> -- отправляет на удаленный репозиторий
git remote -v -- показывает список репозиториев
git reset <commit> -- возвращает коммит к исходному состоянию
git revert <commit> -- создает новый коммит, который отменяет изменения
git stash -- сохраняет изменения во временное хранилище
git stash pop -- возвращает изменения из хранилища
git tag <имя> -- создает тег для коммита
git rebase <ветка> применяет коммит для другой ветки


# Понятие репозитория и структура проекта
репозиторий - это хранилище кода проекта, включающее:
    1. все файлы и папки проекта
    2. историю изменений (коммиты)
    3. информацию о ветках, тегах и настройках

# виды репозиториев:
    1. папка git (скрыта)
    2. удаленный репозиторий
    3. github gitlab

my_project/
│
├── .gitignore              # Файл для игнорирования определенных файлов и папок в Git
├── README.md               # Файл описания проекта (инструкции, как использовать проект)
├── LICENSE                 # Файл лицензии (например, MIT, Apache, GPL и др.)
│
├── src/                    # Исходный код проекта
│   ├── __init__.py         # Инициализационный файл для пакета
│   ├── main.py             # Главный файл приложения
│   └── module1.py          # Дополнительные модули
│
├── tests/                  # Тесты проекта
│   ├── __init__.py         # Инициализационный файл для пакета
│   ├── test_module1.py     # Тесты для module1
│   └── test_module2.py     # Тесты для других модулей
│
├── requirements.txt        # Список зависимостей (для Python-проектов)
├── setup.py                # Скрипт для установки пакета (для Python-проектов)
│
├── docs/                   # Документация проекта
│   ├── index.md            # Главный файл документации
│   └── usage.md            # Инструкции по использованию
│
└── examples/               # Примеры использования вашего проекта
    ├── example1.py         # Пример 1
    └── example2.py         # Пример 2




# Жизненный цикл файлов git
    1. Неотслеживаемая - .gitignore файлы
    2. Измененные - файлы, которые уже были в репозитории, но подверглись изменению
    3. Индексированные - файлы, которые подготовлены к коммиту
    4. Зафиксированная

README лицо проекта


### 22.04.25

# Виды цели и уровни интеграции программных уровней
Интеграция програмных модулей - это процесс объединения отдельных компонентов ПО в единую систему
цель интеграции:
    1. обеспечение взаимодействия модулей
    2. повышение надежности и производительности системы
    3. упрощение разработки и сопровождения ПО
    4. минимизация дублирования функционала

# виды интеграции программных модулей:
    1. по способу взаимодействия:
    горизонатльная интеграция:
        -объеденение модулей одного уровня
    вертикальная интеграция:
        -объеденение модулей разных уровней
    2. по степени связанности
        упрощает замену компонентов
        REST API и микросервисы
    -сильная связанность:
        изменение одного требуе модификации других
        пример: монолитная архитектура
    3. по времени выполнения:
    -статическая интеграция:
        компоненты связываются на этапе компиляции
    -динамическая интеграция:
        компоненты связываются во время выполнения
    
# Уровни интеграции ПО:
    0. уровень данных (БД)
    1. сервисный уровень (API)
    2. уровень пользовательского интерфейса (UI)
    3. Уровень бизнес-логики

Средства сборки и управления зависимостями
CI/CD инструменты
брокеры сообщений



### Автоматизация бизнес-процессов
Это использование технологий для выполения повторяющихся задач, минимизации ручного труда и повышения эффективности работы организации

    1. ускорение выполнения операций
    2. устранение ошибок из-за человеческого фактора
    3. оптимизаци затрат
    4. повышение прозрачности и управляемости процессами
    5. масштабируемость бизнеса

# процессы по сложности:
    1. простые процессы (последовательные задачи)
    2. сложные процессы (условия, циклы, параллельные потоки)

# виды бизнес-процессов по функциональным областям:
    1. равление документооборотом
    2. бухгалтерия
    3. логистика и склад
    4. HR процессы
    5. тех. поддержка

# автоматизация:
    1. базовый уровень (генерация отчетов, обработка данных)
    2. средний уровень (BRM BPM системы)
    3. высокий уровень (имитация действий пользователя)


# Конфиг prettier
{
    # "ключ": "значение"
// максимальное кол-во символов в одной строке до переноса текста
    "printwidth": 80,

// ширина отступа (кол-во пробелов)
    "tabwidth": 3,

// использовать символы в табуляции для отступов
    "useTabs": false,

// ставить точки с запятой в конце выражения javascript
    "semi": true,

// использовать одинарные или двойные кавычки
    "singleQuote": false,

// правила постановки завершающей запятой
// none - не добавлять завершающую запятую
// all - добавлять
// esS - везде, где можно
    "traillingComma": "esS",

// оставлять ли пустое пространство в скобках
    "bracketSpacing": false,

// всегда ли заключать аргументы в стрелочких функция в круглые скобки (slways, avoid)
    "arrowParens": "avoid"
}
SHIFT + ALT + F